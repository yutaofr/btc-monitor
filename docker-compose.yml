services:
  app:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data

  tests:
    build: .
    env_file:
      - .env
    command: ["sh", "-c", "pytest tests/unit tests/e2e && python3 tests/parity/shadow_parity_100_samples.py"]
