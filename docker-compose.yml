services:
  app:
    build: .
    volumes:
      - .:/app
    env_file:
      - .env
    restart: unless-stopped

  tests:
    build: .
    volumes:
      - .:/app
    env_file:
      - .env
    command: pytest tests/unit
